# 문제: VIP 고객 할인율 계산기총 구매 금액을 입력받아, 금액 구간별로 다른 할인율을 적용하고 최종 할인된 금액을 계산하여 반환하는 함수를 만들어 보세요.
# 이 문제는 $\text{if}$문이 위에서부터 순서대로 조건을 검사하고, 하나가 참이면 나머지를 건너뛴다는 원리를 활용하는 데 좋습니다.

# 🌟 요구사항
# 함수 이름: $\text{get_final_price}$로 정해봅시다.
# 매개변수: 총 구매 금액을 나타내는 숫자 $\text{amount}$ 하나를 받도록 정의하세요.
# 수행문장 (할인 로직): 다음 조건을 순서대로 검사해야 합니다.
# VIP 조건 (15% 할인): 총 구매 금액이 100,000원 이상일 때.
# GOLD 조건 (10% 할인): 총 구매 금액이 50,000원 이상일 때.
# SILVER 조건 (5% 할인): 총 구매 금액이 30,000원 이상일 때.
# 일반 조건 (할인 없음): 위의 모든 조건에 해당하지 않을 때.
# 반환값: 할인율이 적용된 최종 결제 금액을 반환($\text{return}$)해야 합니다.

# 🔍 힌트$\text{15\%}$ 할인은 원래 금액에 $\text{0.85}$를 곱하면 됩니다.
# ($\text{1 - 0.15 = 0.85}$)100,000원을 입력받았을 때, 15% 할인이 적용되어야 합니다.
# $\text{elif}$를 사용하면 amount >= 50000 조건을 따로 and amount < 100000처럼 복잡하게 작성할 필요가 없습니다!

def get_final_price(amount):

    if amount >= 100000:
        discount = 0.85
        final_price = amount * discount
        print(f"당신은 VIP입니다 {amount:,}에서 15% 할인 적용")
        return final_price

    elif amount >= 50000:
        discount = 0.90
        final_price = amount * discount
        print(f"당신은 GOLD입니다 {amount:,}에서 10% 할인 적용")
        return final_price
    elif amount >= 30000:
        discount = 0.95
        final_price = amount * discount
        print(f"당신은 SILVER입니다 {amount:,}에서 5% 할인 적용")
        return final_price
    else:
        print(f"할인 조건이 없습니다.")
        return amount

final = get_final_price(10000)
print(f"최종 결제 금액은 {final:,}원 입니다.")

