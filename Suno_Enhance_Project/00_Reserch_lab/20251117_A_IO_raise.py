# 1) raise의 기본 문법**
# raise ValueError("이 값은 유효하지 않습니다. 제가 강제로 발생시킨 에러입니다.")
# print ("이것은 실행될까요?")
import sys


# 2) 함수 안에서 if문과 함께 사용하기 (데이터 검증)

# def set_age(age):
#     if age < 0 or age > 150:
#         raise ValueError("나이는 0과 150 사이의 유효한 값이어야 합니다.")
#     print(f"나이가 {age}값으로 설정 되었습니다.")
#
# set_age(25)
# set_age(156)

# 3) try...except 구문과 함께 사용하기 (예외 다시 발생시키기)
# def file_open_wrapper(filepath):
#     try:
#         f = open(filepath, mode='r')
#         print(f"{filepath} 파일 열기 성공")
#         f.close()
#
#     except FileNotFoundError as e:
#         print(f"[로그] 경고: 파일을 찾을 수 없습니다. (경로:{filepath})")
#         raise
#
# file_open_wrapper("pile.txt")


# -----------------------------------------------------------
# 실습 문제: get_valid_number() 함수 구현
# -----------------------------------------------------------

# -----------------------------------------------------------
# 정답 코드
# -----------------------------------------------------------

def get_valid_number(user_input):
    """사용자로부터 1부터 100 사이의 유효한 정수를 입력받아 반환합니다."""

    while True:
        try:
            user_input = input("1부터 100 사이의 정수를 입력하세요: ")

            # [1] input() 값을 int()로 정수 변환 시도 (ValueError 발생 가능)
            number = int(user_input)  # 정수 변환 시도

            # [2] if + raise를 사용하여 값의 범위 검사 (1 미만이거나 100 초과)
            if number < 1 or number > 100:  # if 조건식을 작성하세요:
                # raise를 사용하여 Exception을 발생시키고 메시지를 전달합니다.
                raise Exception("🚨 입력 값은 1부터 100 사이여야 합니다.")  # Exception 발생시키기

            # [3] 모든 검사를 통과했을 때 (성공)
            print(f"✅ 입력 성공! 최종 숫자: {number}")
            return number

        except ValueError:
            # [4] ValueError 처리 로직 (문자열 입력 시 실행)
            print("🚫 숫자가 아닌 값을 입력하셨습니다. 다시 입력해주세요.")

        except Exception as e:
            # [5] raise로 강제 발생시킨 오류 처리 로직
            #     e 변수에는 raise로 던진 메시지가 담겨 있습니다.
            print(f"🚨 오류 발생: {e} 다시 입력해주세요.")


# -----------------------------------------------------------
# 함수 실행 및 테스트
# -----------------------------------------------------------
get_valid_number(5)
